import '../../utils/dependencies.dart';

class AddNewPlotScreen extends StatelessWidget {
  const AddNewPlotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
