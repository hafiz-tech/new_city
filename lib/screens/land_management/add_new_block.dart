import '../../utils/dependencies.dart';

class AddNewBlockScreen extends StatelessWidget {
  const AddNewBlockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
