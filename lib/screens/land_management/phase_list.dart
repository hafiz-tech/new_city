import '../../utils/dependencies.dart';

class PhaseListScreen extends StatelessWidget {
  const PhaseListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("PHASE LIST"),
    );
  }
}
