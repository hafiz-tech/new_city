import '../../utils/dependencies.dart';

class MonthlyPayrollScreen extends StatelessWidget {
  const MonthlyPayrollScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
