import '../../utils/dependencies.dart';

class PendingChargesListScreen extends StatelessWidget {
  const PendingChargesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
