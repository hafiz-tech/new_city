import '../../utils/dependencies.dart';

class PaidInstallmentListScreen extends StatelessWidget {
  const PaidInstallmentListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
