import '../../utils/dependencies.dart';

class PayServiceChargesScreen extends StatelessWidget {
  const PayServiceChargesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
