import '../../utils/dependencies.dart';

class PaidChargesListScreen extends StatelessWidget {
  const PaidChargesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
